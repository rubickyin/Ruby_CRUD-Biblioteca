<p style="color: green"><%= notice %></p>

<%= render @categorium %>

<div>
  <%= link_to "Editar categoría", edit_categorium_path(@categorium), class: "btn" %> |
  <%= link_to "Ver lista", categoria_path, class: "btn" %>
  </br></br>
  <%= button_to "Eliminar categoría", @categorium, method: :delete, class: "boton" %>
</div>
